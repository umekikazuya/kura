# 03. 詳細設計書（内部設計書）

## 1. アーキテクチャ構成

全体の構造として**クリーンアーキテクチャ**（`app/domain`, `app/usecase`, `app/io`）を採用する。
Go (バックエンド) と React (フロントエンド) を Wails IPCでバインディングし、フロントエンドには描画に必要な最小限のDTOのみを受け渡す。
クリップボード監視には複雑なCGOを避け、保守性の高い Wails APIの定期ポーリングを採用する。

## 2. データベース物理設計 (SQLite FTS5)

パフォーマンスチューニングのため、以下のPRAGMAを設定し、N-gramを用いた仮想テーブルを構築する。

```sql
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA mmap_size = 268435456;

-- メインテーブル
CREATE TABLE clips (
id TEXT PRIMARY KEY,
content TEXT NOT NULL,
content_hash TEXT NOT NULL,
type TEXT NOT NULL,
image_path TEXT,
source_app_bundle_id TEXT,
is_pinned BOOLEAN DEFAULT 0,
created_at INTEGER DEFAULT (cast(strftime('%s','now') as int)),
updated_at INTEGER DEFAULT (cast(strftime('%s','now') as int))
);

-- FTS5仮想テーブル（あいまい検索用）
CREATE VIRTUAL TABLE clips_fts USING fts5(
content,
content_id UNINDEXED,
tokenize='trigram'
);

-- 同期トリガー
CREATE TRIGGER after_clips_insert AFTER INSERT ON clips BEGIN
INSERT INTO clips_fts(content_id, content) VALUES (new.id, new.content);
END;
```


